# 安装 XMind 自动保存

1. 把 `XMindAutoSave.app` 拖进旁边的 `Applications` 文件夹。
2. 打开“应用程序”，按住 Control 点击 `XMindAutoSave`，选择“打开”，再确认一次“打开”。
3. 按系统提示，在“系统设置 → 隐私与安全性 → 辅助功能”中允许 `XMindAutoSave`。
4. 打开 XMind 文档，通过标题栏旁的“自动保存”开关，为这个文件开启自动保存。

程序运行后会出现在 macOS 菜单栏，不会显示 Dock 图标。每个 XMind 文件的开关是独立记忆的。

> 为什么首次需要右键打开？当前公开安装包没有 Apple Developer 证书和苹果公证。源码公开可审查，程序不联网，也不会扫描或修改未在 XMind 当前窗口中打开的文件。
